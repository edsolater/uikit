# Agent 行为准则

## 核心设计哲学

- **代码即配置**：代码本身是行为的唯一真实来源。要禁用或修改功能，直接改调用代码，不靠外部开关、运行时标志或额外配置层绕路控制。
- **优先易用与易读**：成熟项目里新增小功能时，优先选择调用路径最短、读起来最直白的实现。只要关键功能成立，就不要为“可能重复加载”或“可能未来扩展”预埋 bootstrap、manager、guard 之类的额外层级。
- **默认函数式表达**：动作默认直接写成函数调用链。只有当一个对象能被稳定、具体地想象成“一个东西”时，才把它建模成类，例如仓库、货物、上下文、账本。
- **类必须服务直觉理解**：允许类和方法存在，但前提不是“面向对象更规范”，而是“调用者一眼能想象出这个主体在做什么”。如果一个类很难自然说清“它是什么”，说明抽象过头了。
- **类的抽象层级应支持渐进增强**：末端节点类在早期可以先用具体实现直接落地；但一旦某个类开始承担编排、调度、托管、替换其他类的职责，它就不再只是末端节点，而是进入“管理器”角色。进入这一层后，才应考虑把可接入边界抽成 `protocol` / `interface`，避免把单一实现误写成整个体系的固定入口。

## 语言与文档表达

- **本文件结构优先服务 AI 稳定解析，但必须保持人类可读**：[agents.md](agents.md) 应尽量使用稳定标题、清晰分组、单义 bullet 和就近说明，让 AI 易于定位规则边界；但这些结构优化不能把文档写成生硬模板，人类也必须能快速扫读和理解。
- **Markdown 写法默认遵循 [how-to-write-markdown.md](how-to-write-markdown.md)**：像标题层级、无序列表优先、bullet 内部的总结性写法等通用 Markdown 规则，默认以 [how-to-write-markdown.md](how-to-write-markdown.md) 为准；本文件只补充 agent 语义本身需要额外强调的规则。
- **规则文档默认使用跨项目抽象语言**：[agents.md](agents.md) 面向可迁移的工程约束，不应绑定当前仓库里的具体业务主体或名词。除非某条规则天然只服务单一项目，否则优先写成“上层主体 / 下层实现 / 中间层 / 信息体 / 组合层 / 原子能力”这类可复用语言，避免 AI 把规则误解成某个具体业务模型。
- **语言选择按“显示面 / 结构面”硬切分**：显示面统一简体中文，结构面统一英文。
  - **显示面统一简体中文**：包括 `UI`、界面、页面、展示、显示、文案、提示语、按钮文字、标题、表头、说明文字、日志、命令行输出、终端输出、print 输出、报错展示文案、注释、docstring、Markdown 说明。这个项目虽然没有 UI，但所有“直接给人看”的内容都按这一条处理。
  - **结构面统一英文**：包括变量名、函数名、类名、方法名、属性名、模块名、文件名、目录名、配置键、数据字段名、类型名、枚举值、协议字段、表结构字段，以及所有进入代码结构或数据结构边界的命名。
- **拒绝中译英式噪音注释**：中文已经说清时，不要再附无意义英文翻译；如果术语天然更适合英文，就直接用英文术语，不做双语对照。

## 代码修改原则

- **拒绝防御性编码**：不要用默认值、`pop(key, default)`、`try-except` 或多余兜底去掩盖本应暴露的配置错误和逻辑错误。必须存在的输入缺失时，应直接失败。
  - 如果类型注解、配置边界或调用约定已经明确了输入类型，内部就直接按该类型使用，不要为了“不信任外部”做无意义转换。
  - 如果一个值在当前边界已经明确是 `int`、`float`、`str`、`bool`、`number` 这类稳定类型，就不要再手动调用 `int()`、`float()`、`str()`、`bool()`、`Number()`、`String()` 去“再确认一次”。这种重转不是稳健性，而是在制造噪音和表达上的不自信。
  - 只有当数据格式确实与当前语义不匹配时，才做必要转换，例如 numpy 标量落成 Python 标量、向量压成单值、领域对象转换成前端协议格式。
- **必须值必须显式传入**：业务语义上必需的参数应设计成必传项，不要随手给 `None`、`0`、空字符串这类占位默认值。默认值只留给语义上本来就可省略、且省略后仍有唯一明确含义的参数。
- **拒绝魔法数字**：逻辑代码里不要硬编码业务参数，数值应在配置中显式定义。
- **拒绝向后兼容**：重构或重命名后，立即移除旧模式支持，不保留旧路径、旧 fallback、旧开关或双轨分支来“顺便兼容”。
  - **拒绝兼容旧运行方式**：如果已经确认旧运行方式有问题，就直接删除，不再同时维持新旧两套语义。
  - **默认不做过渡层**：除非用户明确要求保留新旧两套运行方式并批准对应文档规则，否则不私自加入兼容层、适配层、过渡分支或 legacy fallback。
- **修改必须顺手收口**：结构性改动应在当轮尽量收成干净结构，不要先塞临时补丁再指望以后回收。已经确认会抬高后续成本的双入口、死分支、占位 wrapper、临时命名，能删就删，能并就并。
- **代码逻辑能够一步优雅地完成目标时，禁止拆成两步**：如果现有主体已经能一步完成目标，就不要再额外包一层“辅助步骤”去重复实现。同一个目标若能直接调用现有能力完成，就不允许为了分层好看再拆成两个彼此依赖的动作。像“自动重排已经发生后再手动重排一次”这类只是在补自己新造流程缺口的逻辑，默认视为冗余骨质增生，应直接删除。
- **拒绝转移式瘦身**：不要为了把某一层代码看起来变薄，就把原本完整的一段复杂逻辑整体搬到另一层，结果只是把组合层或适配层的重量原封不动倒进另一个文件。判断标准不是“某个文件是否变短”，而是“总调用路径是否更短、主体职责是否更清晰、是否减少了为完成同一目标而额外绕出的步骤”。如果搬家后只是换了文件、没有减少步骤和复杂度，那不是重构，只是转移。
- **拒绝凭空引入新概念**：重构时不要为了让解释“自洽”就临时发明新的层级、新名词或新对象。现有主体已经能表达问题时，应直接在现有主体里收口；只有当代码里确实缺少一个不可替代的新主体时，才允许引入新概念。
- **坚持自信编码**：确定逻辑路径上的内部组件应被当作稳定前提直接使用，而不是层层怀疑。
  - **快速失败**：像必需配置项这类确定路径，直接访问即可。若因此抛错，说明系统状态不一致，应修缺陷，而不是补异常分支。
  - **条件判断只处理真实不确定性**：`if` 语句留给外部输入和复杂运行时状态，不要拿来检查本应确定的内部状态。
- **校验优先短 assert**：对于“不成立就会悄悄把数值路径带坏”的不变量，优先使用不带 message 的短 `assert`，让 traceback 直接定位行号。
  - 避免大量 `if ...: raise ...` 和冗长报错字符串，错误定位优先依赖堆栈与全局搜索。
  - 避免无意义缩写，例如 `cfg`；优先使用见名知意的 `*_config` 命名。
- **拒绝数学草稿式代码**：变量名必须清晰可读，避免 `r`、`t`、`p` 这类单字母或不明缩写。核心算法步骤要配合业务白话注释，说明“为什么这么算”，而不只是“在算什么”。
  - 对布尔值、阶段名、状态名这类天然带语言语义的信息，优先直接使用对应的语言类型与字面量，不要退回 `0/1`、含糊缩写或历史习惯写法。

## 命名原则

- **命名的最高准则是见名知意**：代码本身应成为最佳文档，让读者尽量不依赖外部文档也能理解意图。
- **清晰性优先**：优先使用简单、直接、无歧义的词。
- **审慎使用术语**：只有当专业术语比通用描述更精准且广为人知时才使用；避免晦涩或易误解的词。
- **命名必须可想象**：变量名、属性名、方法名不只是“别重名”，而是要让读者一眼看出主体和用途。
- **命名长度优先短而稳**：默认优先 2 到 4 个词，3 个词左右最佳。能用简短结构说清时，不要为了“更完整”堆成 5 个、6 个词；只有少一个词就会丢边界语义时，才允许继续加长。
- **常用缩写可直接保留**：像 `env` 这类在当前领域里约定俗成、几乎不会误解的缩写，不要为了形式统一硬展开成更长单词。缩写是否保留，看的是读者是否会立刻理解，而不是表面上是否“拼写完整”。
- **属性名优先名词表达**：属性表示“对象持有什么”，默认写成名词或名词短语；只有真正代表操作时才用动词式命名。
- **属性名避免伪动作结构**：少用 `*_by_*`、`*_for_*` 这类容易被读成动作说明的结构。优先使用主语明确的名词表述，例如 `active_entity_ids`、`entity_stage_flags`。
- **命名优先表达用途，不表达来源路径**：名字首先回答“它拿来干什么”，不要先回答“它按什么分组”“它从哪里来”“它流向哪里去”。像 `*_by_*`、`*_from_*`、`*_into_*`、`*_snapshot` 这类把来源、分组方式、搬运路径塞进名字里的写法，默认都应回避；优先改成直接说明业务身份或当前职责的名词，例如“本轮分配实体”“当前有效对象”“在架对象”这类直白表达。
- **不要机械排斥有语义的连接词或阶段词**：`info`、`summary`、`for`、`or` 这类词本身不是问题。只要它们在当前位置确实承担清晰语义，就应该保留；真正该避免的是那种既泛又空、删掉也不影响理解的填充词。
- **避免 `item`、`items` 这类过泛占位词**：当代码里已经知道对象是什么时，命名应直接落到真实主体，例如 `orders`、`entities`、`timestamps`，不要再退回语义发空的 `item`、`items`。
- **不要把容器实现细节写进名字**：如果调用方不需要关心底层是 dict、list 还是别的容器，就不要额外加 `table`、`map`、`record` 这类后缀。只有“表 / 队列 / 栈”本身属于语义时才写进名字。
- **语义上是一组对象，就直接命名成那组对象**：例如 `active_entities`、`completed_entities`。不要写成脱离载体的 `activity`、`completion`；即使底层用 dict 模拟 set，也不要把这种实现细节写进名字。
- **语义上是单值，就保持单值形状**：像“当前阶段名”“当前时间戳”“当前模式”这类状态，优先直接用字符串、布尔值、时间戳，不要再套一层容器壳子。
- **已经有明确对象时，不要退回匿名 dict**：代码里已经存在明确主体对象时，优先直接传递和存储对象，而不是退回 `dict[str, Any]`。
- **命名必须让人联想正确**：不仅要大致看懂，还要让第一反应落在正确的业务对象、时间范围和语义边界上。
- **命名必须匹配时间尺度与作用范围**：名字暗示的持续性、覆盖范围、时间长度必须与真实行为一致。例如瞬时结果不要轻率叫 `plan`。
- **命名必须匹配信息阶段**：要分清原始输入、中间判断、最终结果、执行回报分别是什么，不要把“仓位判断”叫成“下单结果”，也不要把“待执行状态”叫成“已执行动作”。
- **避免冗余命名**：几个词已经完整表达语义时，不要再塞重复说明。例如 `start_idx` 和 `end_idx_exclusive` 已经足够，就不要再写成 `range_start_idx`、`range_end_idx_exclusive`。
- **宁可更白话，也不要抽象得让人误会**：命名目标不是显得专业，而是降低误解成本。
- **属性名和方法名要带主语语境**：命名应让调用者自然读成“谁的什么”“谁在做什么”。

## 接口、职责与文件边界

- **文件身份优先于可访问能力**：一个文件能 import 到上游结果、下游实现、配置、环境、缓存、训练参数，不代表它就应该承担这些语义。判断逻辑落点时，先看文件头职责注释写没写这项职责；没写就是白名单外，不能因为“这里拿得到上下文”就顺手塞进来。
- **胶水层默认只做胶水**：像 protocol、bridge、adapter、router、entry、index、facade 这类胶水层，默认只允许做轻量分发、参数转发、结果拼接和边界校验，不允许顺手吸收具体实现里的 bootstrap、状态机、资源初始化、特征组装、训练细节或业务裁决。能拿到上下文，不等于应该在这里落逻辑。
- **权限边界就是修改白名单**：文件头注释、类注释和模块职责里写明的“负责什么 / 不负责什么 / 可以做什么 / 不应做什么”，默认都按硬边界理解，不是可自由发挥的参考说明。后续修改代码时，若新增逻辑不在该白名单内，应先回到正确主体，而不是就地塞进当前文件。
- **先锁定上层约束，再决定下层实现**：像并行规模、阶段边界、最终验收流程归属这类上层事实，必须先确认，再让各个下层实现去服从。
- **下层事实不能倒逼上层语义**：队列长度、并行度、缓存状态、第三方库回调时机，只能说明“当前实现怎么跑”，不能直接推导“需求就该这样改”。
- **谁能看到数据，不等于谁拥有解释权**：某一层拿到了一份上游结果、时间戳、价格序列、窗口边界或摘要信息，只代表它可以消费这些输入，不代表它自动获得了定义这些语义的权力。能读取，不等于能裁决。
- **主体明确时优先对象方法**：当一个动作有明确、单一、稳定的业务主体，并且代码里已有这个主体对象时，优先写成该对象的方法，保持“主语 + 谓语”的自然语义。
- **无稳定主体时再写函数**：函数应表达可复用的通用处理流程，而不是把本该属于某个对象的职责偷出来做成中间工具函数。
- **先分清主体与受体，再决定接口是否存在**：不要因为“流程里需要这一步”就暴露公共方法。先搞清楚是谁在驱动，谁只是被动配合；主体不能倒置，被动对象不应替主动对象做状态裁决。
- **管理类必须先符合协议再接入系统**：如果一个新增类的职责是管理、编排、调度、托管或替换其他类，那么它承担的就是管理器角色。只有进入这一层后，才应考虑把可接入边界抽成 `protocol` / `interface`；简单、意图明确、职责单纯的文件或主体，本身就已经在表达自己的契约，不要为了形式整齐再单独起协议文件。
- **末端类升级为管理类时要同步升级抽象**：一个类起初只是末端执行节点时，可以先保持具体 class；但只要它的职责开始上移，变成上层协调者、组合者或替换点，才应考虑把“class implied interface”显式化，不要继续让调用方依赖那个正在膨胀的具体实现。
- **拒绝过度协议化**：对于意图简单、职责明确、调用方式直白的主体，不要为了“可能未来扩展”先抽一层协议、再单独起一个协议文件。协议说到底只是类型边界；当文件本身已经把职责与边界表达清楚时，就不要再额外复制一份抽象说明。
- **协议文件默认不是优先选项**：只有当业务主体本身复杂、边界混合、接入点不清晰，或者确实存在多个稳定实现需要共享同一接入契约时，才考虑把协议单独写出来。即便需要协议，也优先就近落在当前主题文件里，而不是默认再拆一个新文件。
- **公共接口尽量极简**：如果对象可以根据内部状态自行完成某一步，就不要再要求外界显式通知一次。外界只表达真正的业务动作。
- **组合层不重写内核**：adapter、bridge、proxy 这类组合层只负责拼接现有能力，不应再私自复制一套核心状态机或运算流程。若内核缺少必要能力，应优先补内核本身，而不是在组合层旁路重写一遍。
- **组合层保持轻逻辑**：组合层允许做少量流程编排与结果整合，但前提是它没有创造第二套真相。只要某段状态或算法已经存在唯一内核，组合层就只能调用它，不能在本地再维护一份平行实现。
- **中间层默认禁止语义膨胀**：桥接层、组合层、适配层这类中间层若开始解释阶段边界、时间窗口、业务口径、验收边界或“为什么现在应该是这批数据”，通常就是职责越界。它们默认只消费上层已裁决结果，不负责反向定义业务世界。
- **命名优先服务调用者直觉**：接口名首先要让调用者一眼看懂用途，而不是暴露实现者才懂的底层术语。
- **信息体不要伪装成操作体**：像 `*Info`、`*Summary`、`*Snapshot` 这类对象，本质上只是在承载一个时刻的状态快照，不应随手给它们挂格式化、打印、转换流程这类“看起来像动作”的方法。方法意味着主体自己执行了一步操作，并且天然带有操作前后语义；如果这里只是在外部消费这份信息，就应把相关逻辑放到调用方或独立函数里，而不是硬塞回信息体本身。
- **坚持单文件单职责**：一个文件默认只承载一个稳定职责。展示输出只负责展示，状态落盘只负责持久化；可以并排协作，但不要因为“都要写文件”就揉进同一个文件。
- **文件边界必须忠于职责边界**：不同目的的系统，即使发生在同一主流程里，也应拆成独立文件与独立设施。不要把验收复核、展示输出、状态快照、结果评估这类不同目的的系统混写在一个文件里。
- **遇到分层冲突时，优先保护上层语义主权**：像活动窗口、集合口径、阶段边界、业务裁决这类上层事实，一旦已经有明确归属，就不要再下沉到更底层或中间层。宁可让下层多接一个已裁决结果，也不要让它偷走定义权。

## 注释与文档

- **代码文件开头必须先写职责边界注释**：每个承载稳定业务职责的代码文件，开头都应先用模块级注释或 docstring 写清楚“这个文件负责什么、不负责什么、与相邻文件怎么分工”。这是后续按职责分离维护代码的第一定位点，不能省略。
- **胶水层文件头必须写出禁区**：对于 protocol、bridge、adapter、router、entry、index 这类容易越界的胶水层，文件头不只要写“负责什么”，还要明确写出“即使拿得到相关上下文，也不应承担什么”。注释要能直接阻止后续把具体实现细节继续堆回胶水层。
- **文件开头职责若无法写清楚，或理解起来过度复杂，就先拆文件**：如果一个文件很难在开头用一小段职责边界注释说清楚“它是什么、它不是什么、它和相邻文件如何分工”，默认说明这个文件职责已经混杂。此时优先拆文件，而不是继续把新逻辑塞进去。
- **注释解释设计与职责，不写变更日志**：注释应解释 why 和 how，严禁写“V2.1”“Added by...”这类 Git 提交式历史记录。
- **区域分段注释不使用序号**：区域标题的重点是职责分块，不是阅读顺序。不要写“一、二、三”或“1、2、3”这类编号，避免后续插入新板块时引发整段序号改动和额外 Git 噪音。
- **区域分段注释必须有显眼标识**：当一个文件需要用注释把多个职责区域隔开时，区域标题应使用明显不同于普通说明注释的视觉形式，例如 `# ===== 领域边界建模 =====`；不要只留一行普通文字，否则分区边界不清晰。
- **注释必须分层到位**：类、方法、函数、关键代码块都应有各自层级的注释，不能只靠类头大段说明撑整份文件。
- **方法默认必须有注释或 docstring**：类方法、普通函数、内部工具函数都默认必须在定义附近写清楚“它做什么、输入输出处在什么语义位置、为什么这一步放在这里、它不负责什么”。不要因为是私有方法、内部方法或名字看起来直白就省略；只有那种在局部上下文里几乎不可能误解、且紧邻代码已经把职责说透的极少数一行转发方法，才可以例外。
- **类属性默认都要就近注释**：类里的属性，无论定义在 dataclass、TypedDict、类体还是 `__init__` 里，默认都要在定义附近写清楚“这个属性是什么、承载什么业务语义”。只有那种几乎不可能误解、且在当前上下文里已经被前文严格定义过的属性，才可以极少量省略。
- **属性注释默认补足设计原因**：如果一个属性的存储形状、落点位置、共享方式、生命周期或命名选择不是显而易见的，就要继续就近写清楚“为什么这样放、为什么这样存、它服务哪条职责边界”。维护者应当仅凭属性定义附近的注释，就能判断这个属性为什么存在在这里，而不是在别处。
- **关键字段优先就近注释**：关键属性、字段、状态位应尽量在定义附近解释，让读者看到名字就知道它承载什么业务语义，以及为什么这里需要它。
- **结构型信息体要注释关键字段**：`TypedDict`、`dataclass`、配置体、info 结构、summary 结构等类型，默认要给关键字段写清楚注释；如果字段承担状态摘要、边界快照、统计口径这类容易混淆的职责，还要补一句为什么采用当前口径。只有字段名本身已经强到几乎不会误解时，才可以省略。
- **注释必须明确职责边界**：内级别注释解释当前在做什么、为什么这样分支；方法和函数注释解释职责边界、输入输出和调用预期；类注释解释这个主体是什么、不是什么。
- **注释也是 AI 的语义边界**：AI 对长期上下文和宏观把握不稳定，因此尤其依赖注释识别变量、函数、类的用途与边界。注释不能只写表面动作，必须把职责讲清楚。

## 重构原则：形变神不变

- **形变**：可以大胆优化代码结构、改进命名、升级数据存储形式，例如把 Dict 换成 Vector 以提升性能或对称性。
- **神不变**：绝对不能改变核心业务逻辑、数据流向的因果关系和算法的根本意图。
  - 数据的“流的操作”，例如先模拟后执行、FIFO、风控优先，必须保持原样。
  - 重构是换容器不换药，优化的是载体，保留的是灵魂。
- **先分清什么是重构，什么只是修饰**：重构首先处理的是职责边界、领域归属、调用主语和表达位置，不是表面把代码“修漂亮”。能否称为重构，不看文件是否变短，而看领域划分是否更清楚、职责是否回到正确主体。
- **允许内部路径变化，但禁止改变当前可运行性**：为了把职责收回正确主体，允许修改内部调用路径、逻辑落点、文件归属、转发层次和组合方式。只要系统前后都能正常跑，这类路径变化本身不是问题。
- **禁止把路径变化偷换成运行语义变化**：内部路径可以变，但当前阶段不能借此顺手改掉对外业务含义、主线输入输出协议、稳定产物口径、返回形状、保存/加载约定和现有流程可见的副作用。路径变了不等于目的地能变。
- **判断是否越界时优先看外部可见口径**：一次改动是否允许，不以“我只是换了实现位置”自证，而要检查改动前后是否仍然同样可运行，外部调用方看到的业务语义、产物含义和副作用口径是否保持一致。若这些变了，就不是当前阶段允许的纯重构。
- **重构优先先做领域划分，再谈实现优化**：先回答“这段逻辑属于哪个主体、哪层职责、谁拥有解释权”，再决定代码怎么搬。领域归属未确定时，不应先讨论抽象层好不好看、路径顺不顺、文件是否够薄。
