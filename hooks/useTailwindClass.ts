// this is actually a tailwind class generator. which also add an abstract layer of tailwindClass
// TODO: currently it is just a placeholder
export default function useTailwindClass(config:{forceTailwindClass?:string}) {
  return config.forceTailwindClass
}